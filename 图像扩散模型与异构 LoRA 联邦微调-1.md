# 扩散模型的异构 LoRA 联邦微调



**想要解决的问题：**在不共享原始图文数据的前提下，怎么让多个客户端一起把同一个文生图扩散模型训练好。因为不同客户端的算力和通信条件不一样，所以不让每个客户端都去做代价很高的完整微调，而是只训练适合自己能力的 LoRA 参数，这样可以明显减轻客户端的计算负担和通信开销。最后再由服务器把这些规模不同、贡献也不同的本地更新聚合起来，在节省成本的同时，尽量得到一个效果更好的全局模型。



但是不同的客户端能力不同，LoRA 的 rank 也会不同，直接聚合会出问题。FLoRG 和 LoRA-FAIR，已经说明传统做法里普遍存在两个关键问题：①若直接在 LoRA 双因子空间里做聚合，会带来聚合误差；②即使先在服务器恢复出真实更新再重新分解，也会存在分解漂移，导致不同轮次之间的因子表示不稳定。对于扩散模型来说，还会进一步遇到第三个问题：异构 rank 客户端若从彼此不一致的低秩子空间开始训练，容易造成优化方向不一致，从而影响全局聚合效果。基于这些问题，本文的核心思路是：服务器不直接平均 LoRA 因子而是先恢复客户端真实更新并在更新空间中聚合；聚合后再把结果投影回低秩 LoRA 形式；为了减轻跨轮表示漂移，再引入 Procrustes 对齐；为了适配异构 rank 客户端，再采用嵌套子空间前缀分发。



**核心思想：**

**一：客户端可以用不同大小的LoRA，即异构rank**

​	客户端可以根据自己的设备能力使用不同大小的 LoRA，也就是异构 rank。比如，客户端 $i$ 算力较强，第 $l$ 层 LoRA 的 rank 可以设为 16；客户端 $j$ 算力一般，第 $l$ 层 rank 设为 8；客户端 $k$ 算力较弱，第 $l$ 层 rank 设为 4。这样每个客户端都只训练自己能够承受的那部分低秩参数，而不是被迫使用统一的大 rank。

​	对于第 $l$ 个 LoRA 层，服务器在第 $t$ 轮向客户端下发初始化后的 LoRA 因子，客户端在自己的图文数据上做本地训练，只更新这些 LoRA 层，训练完成后得到本地因子$$B_{i,l}^{(t)},\; A_{i,l}^{(t)}$$

​	因为模型前向传播中真正作用到冻结基座权重上的对象并不是单独的 $B$ 或 $A$，而是它们的乘积，所以客户端在第 $l$ 层的真实低秩更新定义为

​	$$\Delta W_{i,l}^{(t)} = B_{i,l}^{(t)}A_{i,l}^{(t)}$$客户端本地真正学到的内容，用这个真实更新 $\Delta W_{i,l}^{(t)}$ 来表示，而不是只看 LoRA 的两个因子本身。



**二：服务器在聚合时，不是直接平均，而是根据客户端的占比权重，所以第二步计算客户端的权重。**

​	服务器在聚合时，不直接分别平均各客户端上传的 $B_{i,l}^{(t)}$ 和 $A_{i,l}^{(t)}$，因为这样做会引入不同客户端左因子和右因子的交叉项，导致聚合结果偏离真实更新方向。服务器真正应该聚合的对象，是每个客户端恢复出来的真实更新 $\Delta W_{i,l}^{(t)}$。因此，服务器要先为每个客户端在每一层上计算聚合权重，再对真实更新做加权平均。 

在主方法中，聚合权重主要由两部分组成：客户端数据量权重和 rank 校正因子。这样写更稳，也更容易讲清楚方法主线。具体写为

​											$$\alpha_{i,l}^{(t)}=
\frac{n_i\,\eta(r_{i,l})}
{\sum_{j\in S_t} n_j\,\eta(r_{j,l})},
\qquad
\sum_{i\in S_t}\alpha_{i,l}^{(t)}=1$$

​	其中，$n_i$ 表示客户端 $i$ 的本地样本量，$S_t$ 表示第 $t$ 轮参与训练的客户端集合，$r_{i,l}$ 表示客户端 $i$ 在第 $l$ 层采用的本地 rank，$\eta(r_{i,l})$ 表示 rank 校正因子。为了避免高 rank 客户端在异构场景下天然主导聚合，可以取$$\eta(r_{i,l})=\frac{1}{\sqrt{r_{i,l}}}$$这样做的目的不是否认高 rank 客户端的价值，而是避免它们因为参数规模更大而在聚合时自动压过低 rank 客户端。尤其在 Non-IID 场景下，如果不做这样的 rank 校正，极少数高 rank 客户端的更新方向可能会过强地主导全局低秩子空间。





**三：先聚合真实更新，再做低秩重投影和稳定化对齐**





​	服务器在收到来自客户端的本地 LoRA 因子以后，首先恢复每个客户端在第 $l$ 层的真实更新$$\Delta W_{i,l}^{(t)} = B_{i,l}^{(t)}A_{i,l}^{(t)}$$，然后在真实更新空间中做加权聚合:												$$\Delta \bar{W}_{l}^{(t)}
=
\sum_{i\in S_t}
\alpha_{i,l}^{(t)}\Delta W_{i,l}^{(t)}$$

​	服务器聚合完成后，还不能直接把 $\Delta \bar{W}_{l}^{(t)}$ 发回客户端，因为客户端下一轮依然只能训练低秩 LoRA 参数，而不能直接训练一个稠密更新矩阵。因此服务器还要把聚合后的结果重新投影回一个 rank 为 $r_l^g$ 的低秩空间。理论上，这一步可以由截断 SVD 给出；但在扩散模型里，很多层的矩阵维度很大，直接做精确 SVD 计算代价太高，所以在实际实现里采用 Randomized SVD（RSVD）来高效近似这一低秩投影过程。具体可写为：$$\Delta \bar{W}_{l}^{(t)}
\approx
U_l^{(t)}\Sigma_l^{(t)}V_l^{(t)\top}$$然后取前 $r_l^g$ 个主方向，构造新的全局 LoRA 因子:$$\tilde B_l^{(t+1)} = U_l^{(t)}(\Sigma_l^{(t)})^{1/2},
\qquad
\tilde A_l^{(t+1)} = (\Sigma_l^{(t)})^{1/2}V_l^{(t)\top}$$

​	但是，即使服务器已经把聚合后的真实更新重新分解成新的 LoRA 因子，也还会有一个问题：同一个低秩更新矩阵并不只对应一种因子表示。即同一个$$\Delta \bar{W}_{l}^{(t)}$$ 可能对应多组不同的 $(B,A)$ 表达，这样一来如果每轮分解出来的因子表示坐标系不一致，就会导致跨轮初始化不稳定，这就是分解漂移问题。为减轻这种表示漂移，本文在重投影之后，引入正交 Procrustes 对齐，让当前轮得到的 LoRA 因子尽量与上一轮全局因子保持一致。求解一个正交矩阵:$$Q_l^{(t+1)}
=
\arg\min_{Q^\top Q=I}
\left\|
\tilde B_l^{(t+1)}Q-B_l^{(t)}
\right\|_F^2
+
\left\|
Q^\top\tilde A_l^{(t+1)}-A_l^{(t)}
\right\|_F^2$$，定义对齐后的新全局因子为$$B_l^{(t+1)}=\tilde B_l^{(t+1)}Q_l^{(t+1)},
\qquad
A_l^{(t+1)}=(Q_l^{(t+1)})^\top \tilde A_l^{(t+1)}$$

所以$$B_l^{(t+1)}A_l^{(t+1)}
=
\tilde B_l^{(t+1)}Q_l^{(t+1)}(Q_l^{(t+1)})^\top \tilde A_l^{(t+1)}
=
\tilde B_l^{(t+1)}\tilde A_l^{(t+1)}$$，这个对齐过程不会改变乘积本身，即不会改变已经聚合好的真实更新结果。它的作用是让不同轮之间的 LoRA 因子表示更一致，从而提升下一轮的稳定性。$$W_l^{final}=W_l + \Delta \bar{W}_l = W_l + $$$$B_l^{(t+1)}A_l^{(t+1)}$$



​	







**四：服务器根据客户端的 rank 做嵌套子空间前缀分发**

​	得到对齐后的全局 LoRA 因子以后，服务器还要把它发回客户端，供下一轮本地训练使用。因为不同客户端使用的 rank 不一样，所以这里不能简单粗暴地统一发相同大小的 LoRA，也不适合把不够的部分直接当成主方法去零填充。所以采用嵌套子空间前缀分发，对于客户端 $i$ 在第 $l$ 层的本地 rank $r_{i,l}$，服务器不再重新为每个客户端单独构造一个互不相同的子空间，而是直接把全局因子的前 $r_{i,l}$ 个主方向前缀分发给它：$$B_{i,l}^{(t+1,0)}=B_l^{(t+1)}[:,1:r_{i,l}], 
A_{i,l}^{(t+1,0)}=A_l^{(t+1)}[1:r_{i,l},:]$$

也就是说，rank 小的客户端收到的是全局主方向的短前缀，rank 大的客户端收到的是更长的前缀。这样所有客户端虽然 rank 不同，但它们的初始化都来自同一个全局主子空间，只是长度不同。这种前缀分发方式，可以显著缓解异构 rank 下的子空间失配问题，因为在强 Non-IID 场景下，低 rank 客户端仍然可能只覆盖全局主空间中的局部方向，所以这更应看作一种结构先验很强、实践上较稳的初始化策略。





五、实验评估

FID，用来评价生成图像分布和真实图像分布之间的差异；CLIPScore，用来衡量生成图像和输入文本之间的语义对齐程度；client-wise FID variance，用来观察不同客户端个体性能之间的离散程度；同时还要统计客户端可训练参数量、单轮上行/下行通信字节数、服务器单轮聚合时间和总 wall-clock time，以验证该方法在异构设备条件下的通信和计算代价是否可控。



补充解释：

​		①层$l$的解释：第 $l$ 个被插入 LoRA 的线性权重模块。每一个插入LoRA的线性变换模块，都算做一个LoRA层，U-Net 里有很多 attention block（down blocks、mid block、up blocks），如果在每个 attention block 的 to_q , to_k , to_v , to_out 上都插 LoRA，那每个 block 就贡献 4 个 LoRA 层。核心层：认为对生成质量更关键的 LoRA 层，普通层：中等重要性的层，次要层：影响相对没那么大的 LoRA 层。

​				    

![image-20260415101744017](E:\software\Typora.1.12.2\Typora\images\image-20260415101744017.png)





参考文献：

​	[1]FLoRG: Federated Fine-tuning with Low-rank Gram Matrices and Procrustes Alignment

​	[2]LoRA-FAIR: Federated LoRA Fine-Tuning with Aggregation and Initialization Refinement

​	[3]Training diffusion models with federated learning

​	[4]Why Diffusion Models Don’t Memorize: The Role of Implicit Dynamical Regularization in Training